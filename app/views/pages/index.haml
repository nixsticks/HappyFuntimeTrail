.center.page-header

.hero
  .row
    %h1{class: "col-md-9"} Make trails. Add treasure. Share with friends.

.section

  
.section


- if signed_in?
  = render 'signed_in'
- else
  = render 'signed_out'