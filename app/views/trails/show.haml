= content_tag :div, class: "pin-data", data: {pins: @trail.pins.order('stepnumber')} do |pin|
  - nil

%h1= @trail.name
%p= @trail.description

#map-canvas

%button
  = link_to 'Start Trail', trail_start_path(@trail), method: :post

= javascript_include_tag params[:controller]